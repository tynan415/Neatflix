import {connect} from 'react-redux';
import SplashBar from './splash_bar';

const mapStateToProps = (state) => ({
  
    
});

const mDTP = (dispatch) => ({

})

export default connect(mapStateToProps, mDTP)(SplashBar);